# Relative path so the Vite dev-server proxy (vite.config.ts) forwards
# requests to the Spring Boot API, avoiding CORS. Use the full URL
# (e.g. http://localhost:8080/api) only if the backend allows CORS.
VITE_API_BASE_URL=/api
